
  # Update HTML Prototype

  This is a code bundle for Update HTML Prototype. The original project is available at https://www.figma.com/design/qP5MAT8m9eklIjcSwMnOQ9/Update-HTML-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  